# 채팅 프로젝트 리팩토링 계획

## 📊 현재 상황 분석

### 파일 크기
- `chat_screen.dart`: **2,352줄** (매우 긴 파일)
- `chat_list_screen.dart`: **406줄** (적당한 크기)

### 기존 폴더 구조
```
intersection-frontend/lib/screens/chat/
├── chat_list_screen.dart
├── chat_screen.dart
├── dialogs/          (이미 존재)
├── services/         (이미 존재)
├── utils/            (이미 존재)
└── widgets/          (이미 존재)
```

---

## 🎯 리팩토링 목표

1. **`chat_screen.dart`를 기능별로 분리하여 가독성 향상**
2. **기존 기능 100% 유지 (절대 건드리지 않음)**
3. **재사용 가능한 컴포넌트로 분리**
4. **유지보수성 향상**

---

## 📋 분리 계획

### 1. **위젯 분리** (`widgets/` 폴더)

#### 1.1 `chat_header.dart` (헤더 위젯)
- **기능**: AppBar의 프로필 이미지, 이름, 검색 모드 토글
- **예상 줄 수**: ~150줄
- **포함 내용**:
  - 프로필 이미지 및 이름 표시
  - 검색 모드 토글 버튼
  - 검색 입력 필드
  - 더보기 메뉴 (차단/신고/나가기)

#### 1.2 `status_banner.dart` (상태 배너 위젯)
- **기능**: 차단/신고/나감 상태 배너 표시
- **예상 줄 수**: ~100줄
- **포함 내용**:
  - 내가 신고/차단한 경우 배너
  - 상대방이 나를 신고/차단한 경우 배너
  - 상대방이 채팅방을 나간 경우 배너

#### 1.3 `pinned_message_bar.dart` (고정 메시지 바)
- **기능**: 고정된 메시지 표시 및 순환
- **예상 줄 수**: ~80줄
- **포함 내용**:
  - 고정 메시지 한 줄 표시
  - 클릭 시 메시지로 이동 및 순환

#### 1.4 `message_bubble.dart` (메시지 버블)
- **기능**: 개별 메시지 버블 렌더링
- **예상 줄 수**: ~250줄
- **포함 내용**:
  - 텍스트 메시지 표시
  - 이미지 메시지 표시
  - 파일 메시지 표시
  - 전화번호 메시지 표시
  - 카운트다운 및 삭제 버튼
  - 읽음/안읽음 표시

#### 1.5 `message_input_field.dart` (메시지 입력 필드)
- **기능**: 메시지 입력 및 전송 UI
- **예상 줄 수**: ~200줄
- **포함 내용**:
  - 텍스트 입력 필드
  - 이모지 피커 토글
  - 첨부 옵션 버튼
  - 전송 버튼
  - 차단 상태에 따른 비활성화

#### 1.6 `emoji_picker_widget.dart` (이모지 피커)
- **기능**: 이모지 선택 UI
- **예상 줄 수**: ~50줄
- **포함 내용**:
  - EmojiPicker 위젯 래핑

---

### 2. **서비스 분리** (`services/` 폴더)

#### 2.1 `chat_message_service.dart` (메시지 관리 서비스)
- **기능**: 메시지 로딩, 필터링, 검색
- **예상 줄 수**: ~150줄
- **포함 내용**:
  - `_loadMessages()` 메서드
  - `_filterMessages()` 메서드
  - `_updateFilteredMessages()` 메서드
  - `_toggleSearchMode()` 메서드

#### 2.2 `chat_send_service.dart` (메시지 전송 서비스)
- **기능**: 다양한 타입의 메시지 전송
- **예상 줄 수**: ~400줄
- **포함 내용**:
  - `_sendMessage()` - 텍스트 메시지
  - `_pickAndSendImage()` - 이미지 선택 및 전송
  - `_takePictureAndSend()` - 사진 촬영 및 전송
  - `_pickFile()` - 파일 선택 및 전송
  - `_sendPhoneNumber()` - 전화번호 전송
  - `_showAttachmentOptions()` - 첨부 옵션 표시

#### 2.3 `chat_file_service.dart` (파일 처리 서비스)
- **기능**: 파일 다운로드 및 이미지 뷰어
- **예상 줄 수**: ~200줄
- **포함 내용**:
  - `_downloadFile()` - 파일 다운로드 (웹/모바일)
  - `_showImageViewer()` - 이미지 뷰어 표시

#### 2.4 `chat_scroll_service.dart` (스크롤 관리 서비스)
- **기능**: 스크롤 제어 및 메시지로 이동
- **예상 줄 수**: ~150줄
- **포함 내용**:
  - `_scrollToBottom()` - 맨 아래로 스크롤
  - `_scrollToMessage()` - 특정 메시지로 스크롤
  - `_scrollToMessageAfterLoad()` - 로딩 후 메시지로 스크롤
  - `_onScroll()` - 스크롤 리스너

#### 2.5 `chat_pin_service.dart` (고정 메시지 서비스)
- **기능**: 고정 메시지 관리
- **예상 줄 수**: ~100줄
- **포함 내용**:
  - 고정 메시지 목록 관리
  - 고정 메시지 순환 로직
  - 고정 메시지로 스크롤

#### 2.6 `chat_timer_service.dart` (타이머 관리 서비스)
- **기능**: 메시지 카운트다운 타이머 관리
- **예상 줄 수**: ~100줄
- **포함 내용**:
  - `_startMessageCountdown()` - 카운트다운 시작
  - `_deleteMessage()` - 메시지 삭제
  - 타이머 취소 및 정리

#### 2.7 `chat_block_service.dart` (차단/신고 서비스)
- **기능**: 차단/신고 상태 확인 및 관리
- **예상 줄 수**: ~100줄
- **포함 내용**:
  - `_checkBlockStatus()` - 차단 상태 확인
  - `_checkReportStatus()` - 신고 상태 확인

---

### 3. **다이얼로그 분리** (`dialogs/` 폴더)

#### 3.1 `block_dialogs.dart` (차단 관련 다이얼로그)
- **기능**: 차단/차단 해제 다이얼로그
- **예상 줄 수**: ~150줄
- **포함 내용**:
  - `_showBlockDialog()` - 차단 확인 다이얼로그
  - `_showUnblockDialog()` - 차단 해제 확인 다이얼로그
  - `_showBlockedDialog()` - 차단 상태 안내 다이얼로그

#### 3.2 `report_dialogs.dart` (신고 관련 다이얼로그)
- **기능**: 신고/신고 취소 다이얼로그
- **예상 줄 수**: ~150줄
- **포함 내용**:
  - `_showReportDialog()` - 신고 확인 다이얼로그
  - `_showUnreportDialog()` - 신고 취소 확인 다이얼로그

#### 3.3 `leave_chat_dialog.dart` (채팅방 나가기 다이얼로그)
- **기능**: 채팅방 나가기 확인
- **예상 줄 수**: ~50줄
- **포함 내용**:
  - `_showLeaveChatDialog()` - 나가기 확인 다이얼로그

#### 3.4 `message_menu_dialog.dart` (메시지 메뉴 다이얼로그)
- **기능**: 메시지 복사/고정 메뉴
- **예상 줄 수**: ~100줄
- **포함 내용**:
  - `_showPinMenu()` - 메시지 메뉴 표시
  - `_copyMessage()` - 메시지 복사
  - `_copyAndPinMessage()` - 복사 및 고정

---

### 4. **유틸리티 분리** (`utils/` 폴더)

#### 4.1 `chat_formatters.dart` (포맷터)
- **기능**: 시간 포맷팅 등
- **예상 줄 수**: ~30줄
- **포함 내용**:
  - `_formatTime()` - 시간 포맷팅

---

## 📁 최종 파일 구조

```
intersection-frontend/lib/screens/chat/
├── chat_list_screen.dart          (406줄 - 유지)
├── chat_screen.dart                (~400줄로 축소 예정)
│
├── widgets/
│   ├── chat_header.dart            (~150줄)
│   ├── status_banner.dart          (~100줄)
│   ├── pinned_message_bar.dart     (~80줄)
│   ├── message_bubble.dart         (~250줄)
│   ├── message_input_field.dart    (~200줄)
│   └── emoji_picker_widget.dart    (~50줄)
│
├── services/
│   ├── chat_message_service.dart   (~150줄)
│   ├── chat_send_service.dart      (~400줄)
│   ├── chat_file_service.dart      (~200줄)
│   ├── chat_scroll_service.dart    (~150줄)
│   ├── chat_pin_service.dart       (~100줄)
│   ├── chat_timer_service.dart    (~100줄)
│   └── chat_block_service.dart     (~100줄)
│
├── dialogs/
│   ├── block_dialogs.dart          (~150줄)
│   ├── report_dialogs.dart         (~150줄)
│   ├── leave_chat_dialog.dart      (~50줄)
│   └── message_menu_dialog.dart    (~100줄)
│
└── utils/
    └── chat_formatters.dart        (~30줄)
```

---

## 🔄 리팩토링 진행 순서

### Phase 1: 유틸리티 분리 (가장 안전)
1. `utils/chat_formatters.dart` 생성
2. `_formatTime()` 메서드 이동
3. `chat_screen.dart`에서 import 및 사용

### Phase 2: 위젯 분리 (UI 컴포넌트)
1. `widgets/status_banner.dart` 생성
2. `widgets/pinned_message_bar.dart` 생성
3. `widgets/message_bubble.dart` 생성
4. `widgets/message_input_field.dart` 생성
5. `widgets/chat_header.dart` 생성
6. `widgets/emoji_picker_widget.dart` 생성
7. 각 위젯을 `chat_screen.dart`에서 import 및 사용

### Phase 3: 서비스 분리 (비즈니스 로직)
1. `services/chat_message_service.dart` 생성
2. `services/chat_scroll_service.dart` 생성
3. `services/chat_pin_service.dart` 생성
4. `services/chat_timer_service.dart` 생성
5. `services/chat_block_service.dart` 생성
6. `services/chat_file_service.dart` 생성
7. `services/chat_send_service.dart` 생성
8. 각 서비스를 `chat_screen.dart`에서 import 및 사용

### Phase 4: 다이얼로그 분리
1. `dialogs/block_dialogs.dart` 생성
2. `dialogs/report_dialogs.dart` 생성
3. `dialogs/leave_chat_dialog.dart` 생성
4. `dialogs/message_menu_dialog.dart` 생성
5. 각 다이얼로그를 `chat_screen.dart`에서 import 및 사용

---

## ⚠️ 주의사항

### 1. **기존 기능 100% 유지**
- 모든 메서드의 시그니처와 동작을 정확히 유지
- 상태 관리 로직 변경 금지
- API 호출 방식 변경 금지

### 2. **의존성 관리**
- 위젯은 필요한 데이터를 파라미터로 받도록 설계
- 서비스는 필요한 상태를 파라미터로 받거나 콜백으로 처리
- Context는 위젯에서만 사용, 서비스에서는 사용 금지

### 3. **테스트 전략**
- 각 Phase 완료 후 즉시 테스트
- 기능별로 하나씩 분리하고 테스트
- 문제 발생 시 즉시 롤백

### 4. **상태 관리**
- `_ChatScreenState`의 상태 변수들은 그대로 유지
- 분리된 서비스/위젯은 상태를 파라미터로 받도록 설계
- setState는 `chat_screen.dart`에서만 호출

---

## 📊 예상 효과

### Before
- `chat_screen.dart`: **2,352줄** (단일 파일)

### After
- `chat_screen.dart`: **~400줄** (메인 로직만)
- 분리된 파일들: **~2,200줄** (기능별로 분산)

### 장점
1. ✅ 가독성 향상 (각 파일이 명확한 역할)
2. ✅ 유지보수성 향상 (수정 시 해당 파일만 찾으면 됨)
3. ✅ 재사용성 향상 (위젯/서비스 재사용 가능)
4. ✅ 테스트 용이성 향상 (단위 테스트 작성 가능)
5. ✅ 협업 효율성 향상 (파일별로 작업 분담 가능)

---

## ✅ 진행 전 확인사항

1. **기존 기능 테스트 완료 여부 확인**
2. **각 Phase별 테스트 계획 수립**
3. **롤백 계획 수립**
4. **사용자 승인 대기**

---

## 🚀 다음 단계

사용자 승인 후 Phase 1부터 순차적으로 진행합니다.

