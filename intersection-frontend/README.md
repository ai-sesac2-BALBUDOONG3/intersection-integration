# intersection-frontend (Flutter)

학창 시절 친구들과 재연결하는 소셜 네트워크 플랫폼 - Flutter 앱

## 🚀 빠른 시작

### 필수 요구사항
- Flutter SDK 3.10+
- Dart 3.0+
- Android Studio / Xcode (모바일 개발 시)
- Chrome (웹 개발 시)

### 설치 및 실행

```bash
# 패키지 설치
flutter pub get

# Android 에뮬레이터에서 실행
flutter run -d emulator-5554

# Chrome에서 실행
flutter run -d chrome

# iOS 시뮬레이터에서 실행 (macOS만)
flutter run -d ios
```

## 🔧 백엔드 연결

백엔드 서버가 실행 중이어야 합니다:
- **백엔드 저장소**: `../intersection-backend`
- **서버 주소**: http://127.0.0.1:8000

API 설정은 자동으로 플랫폼에 맞게 조정됩니다:
- **웹**: `http://127.0.0.1:8000`
- **Android 에뮬레이터**: `http://10.0.2.2:8000`

설정 파일: `lib/config/api_config.dart`

## 📂 프로젝트 구조

```
lib/
├── main.dart                 # 앱 진입점
├── config/                   # 설정
│   └── api_config.dart      # API URL 설정
├── data/                     # 데이터 관리
│   ├── app_state.dart       # 글로벌 상태
│   ├── user_storage.dart    # 로컬 저장소
│   └── signup_form_data.dart
├── models/                   # 데이터 모델
│   ├── user.dart
│   └── post.dart
├── services/                 # API 서비스
│   └── api_service.dart     # HTTP 요청
├── screens/                  # UI 화면
│   ├── auth/                # 인증 (로그인/회원가입)
│   ├── signup/              # 회원가입 단계
│   ├── profile/             # 프로필
│   ├── friends/             # 친구 목록/추천
│   ├── community/           # 커뮤니티 게시판
│   ├── chat/                # 채팅
│   └── common/              # 공통 화면
└── widgets/                  # 재사용 위젯
    ├── fire_like_button.dart
    └── liquid_button.dart
```

## 🎨 주요 기능

- ✅ 회원가입 / 로그인
- ✅ 카카오 OAuth (실제 + 개발용)
- ✅ 프로필 설정 (이름, 생년, 지역, 학교)
- ✅ 친구 추천 (동일 지역/학교 기준)
- ✅ 커뮤니티 게시판 (게시물/댓글)
- ✅ 실시간 친구 목록
- ✅ 자동 로그인 (토큰 저장)
- ✅ 멀티 플랫폼 (Web, Android, iOS)

## 🔑 카카오 로그인

3가지 로그인 방법:
1. **일반 로그인**: 이메일/비밀번호
2. **카카오 로그인**: 실제 카카오 OAuth (백엔드 설정 필요)
3. **개발용 로그인**: 카카오 설정 없이 즉시 로그인

## 🛠️ 개발

### Hot Reload
앱 실행 중 터미널에서 `r` 키 입력

### 빌드

```bash
# Android APK
flutter build apk

# iOS (macOS만)
flutter build ios

# Web
flutter build web
```

## 📦 주요 패키지

- `http`: HTTP 요청
- `shared_preferences`: 로컬 저장소
- `flutter_web_auth`: OAuth 인증
- `file_picker`: 파일 선택

## 🐛 문제 해결

### 네트워크 에러 (Android)
- 백엔드가 실행 중인지 확인
- `AndroidManifest.xml`에 인터넷 권한이 있는지 확인 (이미 추가됨)

### 카카오 로그인 실패
- 백엔드의 `.env` 파일 확인
- 카카오 개발자 콘솔에서 Redirect URI 확인
- 또는 "개발용 로그인" 버튼 사용

### Not authenticated
- 로그인 후 토큰이 저장되었는지 확인
- `app_state.dart`에서 토큰 로드 확인

## 🔒 보안

✅ **gitignore에 포함**:
- `build/`
- `.dart_tool/`
- `*.log`
- `.env` (백엔드)

## 📱 테스트 계정

개발용 로그인 시 자동으로 테스트 계정 생성됩니다.

## 📚 Flutter 리소스

- [Flutter 공식 문서](https://docs.flutter.dev/)
- [Dart 언어 튜토리얼](https://dart.dev/tutorials)
- [Flutter Cookbook](https://docs.flutter.dev/cookbook)
